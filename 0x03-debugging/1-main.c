#include <stdio.h>
/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	print("Infinite loop incoming :(\n");

	i = 0;
	/**
	 * while (i < 10)
	 * {
	 * _putcha(i):
	 * }
	 */
	printf("infinite loop avoided: \\o/\n");

	return (0);
}
